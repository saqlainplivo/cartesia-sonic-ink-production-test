# Web server & async
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
httpx>=0.27.0
python-multipart>=0.0.9

# Plivo
plivo>=4.50.0

# Cartesia
cartesia>=1.0.0

# Audio processing
numpy>=1.26.0
scipy>=1.13.0          # μ-law / G.711 codec helpers

# STT + WER
openai-whisper>=20231117   # local whisper (STT_PROVIDER=whisper_local)
jiwer>=3.0.3               # WER calculation
deepgram-sdk>=3.2.7        # optional (STT_PROVIDER=deepgram)
assemblyai>=0.26.0         # optional (STT_PROVIDER=assemblyai)

# Config & utilities
python-dotenv>=1.0.1
pydantic-settings>=2.3.0
rich>=13.7.0               # pretty console output
tabulate>=0.9.0            # results table formatting
